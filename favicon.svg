<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32">
  <!-- Dark background -->
  <rect width="32" height="32" fill="#090806" rx="2"/>

  <!-- Horizontal bar (ember = the backlight) -->
  <rect x="5" y="7" width="22" height="3" fill="#ca6630" rx="1"/>

  <!-- Vertical stem (cream = the subject against the light) -->
  <rect x="13" y="10" width="6" height="16" fill="#f8f4ec" rx="1"/>
</svg>